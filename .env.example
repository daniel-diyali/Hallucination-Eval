# Copy this file to `.env` and fill in real keys. The real .env is
# gitignored so the keys never leave my machine.

# Anthropic (Claude). Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-replace-me

# OpenAI. Get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-replace-me

# Google (Gemini). Get one at https://aistudio.google.com/apikey
GOOGLE_API_KEY=replace-me
